from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("jobs", "0001_initial"),
    ]

    operations = [
        # Fields already present in 0001 (schema refreshed); keep migration for compatibility
    ]
